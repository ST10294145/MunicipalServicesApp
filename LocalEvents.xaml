<Window x:Class="MunicipalServicesApp.LocalEvents"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Local Events and Announcements" Height="500" Width="800">

    <Grid Margin="20">
        <StackPanel>
            <TextBox x:Name="txtSearch"
                     Width="300"
                     Margin="0,0,0,10"
                     Text="Search events..."
                     Foreground="Gray"
                     GotFocus="txtSearch_GotFocus"
                     LostFocus="txtSearch_LostFocus"
                     TextChanged="txtSearch_TextChanged"/>

            <ComboBox x:Name="cmbCategory" Width="200" Margin="0,0,0,10"/>
            <DatePicker x:Name="dpDate" Width="200" Margin="0,0,0,10"/>
            <Button x:Name="btnSearch" Content="Search" Width="120" Click="btnSearch_Click"/>

            <ListBox x:Name="lstSuggestions"
         Visibility="Collapsed"
         Height="100"
         Margin="10,60,10,0"
         Background="White"
         BorderBrush="Gray"
         BorderThickness="1"
         MouseLeftButtonUp="lstSuggestions_MouseLeftButtonUp" />


            <DataGrid x:Name="dgEvents"
                      Height="200"
                      Margin="0,10,0,10"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>

            <Label Content="Recommended based on your searches:" FontWeight="Bold"/>
            <ListBox x:Name="lstRecommendations" Height="100"/>
        </StackPanel>
    </Grid>
</Window>
