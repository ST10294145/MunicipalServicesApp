<Window x:Class="MunicipalServicesApp.LocalEvents"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Local Events and Announcements" Height="731" Width="780"
        WindowStartupLocation="CenterScreen">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,0,59,34">
        <Grid Margin="20">
            <StackPanel>

                <!-- Add Event Button (Visible only for Admin) -->
                <Button x:Name="btnAddEvent"
                        Content="➕ Add New Event"
                        Width="180"
                        Height="35"
                        Background="#FF007ACC"
                        Foreground="White"
                        FontWeight="Bold"
                        Margin="0,0,0,15"
                        Visibility="Visible"
                        Click="btnAddEvent_Click"/>

                <!-- Back Button -->
                <Button x:Name="btnBack"
                        Content="← Back to Main Menu"
                        Width="180"
                        Height="35"
                        HorizontalAlignment="Left"
                        Margin="0,0,0,15"
                        Background="#FF007ACC"
                        Foreground="White"
                        FontWeight="Bold"
                        Click="btnBack_Click"/>

                <!-- Search Bar -->
                <TextBox x:Name="txtSearch"
                         Width="300"
                         Margin="0,0,0,10"
                         Text="Search events..."
                         Foreground="Gray"
                         GotFocus="txtSearch_GotFocus"
                         LostFocus="txtSearch_LostFocus"
                         TextChanged="txtSearch_TextChanged"/>

                <!-- Category Filter -->
                <ComboBox x:Name="cmbCategory"
                          Width="200"
                          Margin="0,0,0,10"/>

                <!-- Date Filter -->
                <DatePicker x:Name="dpDate"
                            Width="200"
                            Margin="0,0,0,10"/>

                <!-- Search Buttons Row -->
                <StackPanel Orientation="Horizontal">
                    <Button x:Name="btnSearch"
                            Content="Search"
                            Width="120"
                            Height="35"
                            Background="#FF28A745"
                            Foreground="White"
                            FontWeight="Bold"
                            Margin="0,0,10,10"
                            Click="btnSearch_Click"/>

                    <Button x:Name="btnReset"
                            Content="Clear Search"
                            Width="120"
                            Height="35"
                            Background="#FFFF5722"
                            Foreground="White"
                            FontWeight="Bold"
                            Margin="0,0,0,10"
                            Click="btnReset_Click"/>
                </StackPanel>

                <!-- DataGrid for Events -->
                <DataGrid x:Name="dgEvents"
                          Height="250"
                          Margin="0,10,0,10"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          GridLinesVisibility="Horizontal"
                          AlternatingRowBackground="#FFEFEFEF">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="150"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date}" Width="120"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                    </DataGrid.Columns>
                </DataGrid>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</Window>
